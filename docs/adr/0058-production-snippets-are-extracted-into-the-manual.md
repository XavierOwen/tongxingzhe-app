# 正式代码片段自动提取到说明书

说明书中声称来自当前 App 的 Dart 和 SQL 示例，以稳定的命名标记从真实正式源文件或可执行测试文件中自动提取，不在 Markdown 中人工维护一份容易过期的副本；引用标记失效时，文档构建必须失败。只有为教学特意改写的片段才直接保存在 Markdown 中，并明确标注为“简化示例”；可执行的 SQL 教学示例同时作为自动化测试输入，以验证它们与当前数据库结构仍然一致。
