# 平台治理规范区域树

全 App 共用的规范区域树由平台区域维护者审核和维护，组织或项目管理员只能提交新增节点与边界修正建议，不能直接改变全局父级关系。组织可以设置自己的显示别名，但别名不改变区域身份。建议审核期间，接触仍归入已有的最小上级；规范树更新后可以按新版本重新解析已有坐标，同时保留原区域结果与解析版本。分析必须同时支持按当前规范树重新归类的当前区域视图，以及按记录发生时版本复现的原始区域视图；日常图表默认使用当前区域视图，原始区域视图用于历史复现和审计，确保跨期比较与历史证据都成立。
