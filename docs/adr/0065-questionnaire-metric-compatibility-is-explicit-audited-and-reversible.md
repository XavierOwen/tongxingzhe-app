# 问卷指标兼容决定必须明确、可审计且可撤销

只有拥有 `manage_analysis_definitions` 成员能力的项目成员才能确认问卷版本语义兼容；发布时必须并排比较定义、选项、时间范围与回答方式，显式选择兼容或不兼容，填写理由并预览对趋势和样本数的影响。该决定保留操作者、时间、理由和版本范围的审计历史并可撤销；撤销后动态报表重新分开计算，已导出报告保留当时定义。组织可选启用第二人批准，但平台默认不强制双人审批，个人空间由本人决定。
