# 版本化指标目录统一各分析层的解释

每个正式统计指标在指标目录中只有一份权威语义定义，包含稳定标识、指标版本、统计单位、分子与分母、排除项、时间口径和隐私规则；Flutter、后端 SQL 与分析仓库 SQL 可使用适合各自环境的实现，但必须声明相同指标版本，并使用同一组虚构输入与预期结果完成跨层对账测试。界面不在 Widget 中临时拼装公式，说明书和报告快照都引用明确指标版本；修复实现错误须保留更正记录，含义改变则创建新版本。
