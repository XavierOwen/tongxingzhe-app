# 关联对象时明确确认建立项目关系

在接触记录中选择同一空间内、但尚未加入当前推广项目的对象时，App 必须在当前记录流程内明确请求确认，不得静默建立项目关系，也不得迫使用户离开记录页另行操作。用户确认后，系统以关系阶段 `0` 建立对象与当前项目的项目关系，并完成此次接触对象关联；不能根据单次兴趣或对象当次反应自动推断更高的长期关系阶段。

用户如不确认，可以取消该对象关联，并继续把本次接触保存为匿名记录或只关联其他对象。这个设计让“关联”带来的项目归属变化保持可见，同时不破坏快速记录流程。
