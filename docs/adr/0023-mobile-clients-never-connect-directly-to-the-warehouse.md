# 移动端不直连分析仓库

Flutter 客户端只通过 Backend API 与云端交互，不能持有 Snowflake 一类分析仓库的凭据或直接执行仓库 SQL。后端事务数据库负责认证相关状态、权限、同步、问卷验证和当前业务数据，再把去身份化分析事实送入可替换的分析仓库；聚合结果也经 API 返回 App。SQL 学习分别落在本地 SQLite、后端事务数据库和分析仓库三层，而不以牺牲安全边界为代价。
