# 共用层级区域与最终解析

同行者使用全 App 共用的严格层级区域树，每个区域节点只有一个规范父级。城市是面对面接触最终归属的最低保障层级，街道、学校、商店等可以成为更具体的节点；“校园”“超市”等区域属性只描述节点，不构成额外父级或汇总路径。接触记录只关联当时能确定的最小区域，上级片区、城市和州通过唯一父级链推导。离线或解析服务暂不可用时，可以先保存经纬度与待解析状态，但最终必须归入至少城市；非线下接触明确使用 `N/A`，不参与区域解析。
