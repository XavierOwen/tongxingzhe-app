# 稳定核心搭配版本化可配置问卷

同行者把时间、渠道、接触地点、触达人数和单次兴趣等跨场景事实作为稳定核心，把年龄、身份和场景特有问题放入推广项目可配置的场景问卷。管理员可以在不发布新版 App 的情况下为推广项目发布新问卷版本，接触记录引用填写时的固定版本，旧数据不会因后续配置变化而改变含义。Flutter 根据受支持的题型渲染并验证问卷，SQL 保留可直接分析的核心列，并以带类型和版本的结构保存回答，而不是把全部数据压成缺少约束的 JSON。
