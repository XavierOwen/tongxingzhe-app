name: tongxingzhe_app
description: "A six-platform promotion contact and self-accountability app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  geolocator: ^14.0.3
  flutter_timezone: ^5.1.0
  flutter_secure_storage: ^10.3.1
  flutter_local_notifications: ^22.2.0
  timezone: ^0.11.1
  crypto: ^3.0.7
  drift: ^2.34.0
  drift_flutter: ^0.3.0
  http: ^1.6.0
  supabase_flutter: ^2.16.0
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.34.0
  build_runner: ^2.15.0

flutter:
  uses-material-design: true
